# Extra dependencies for the tree baseline submission.
# The core project dependencies (scikit-learn, numpy, pandas, joblib)
# are already installed via the project's pyproject.toml.
# Add any additional packages below if needed.
