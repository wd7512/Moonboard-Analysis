# Extra dependencies for the LSTM baseline submission.
# The core project dependencies (torch, scikit-learn, pandas, numpy, mlflow)
# are already installed via the project's pyproject.toml.
# Add any additional packages below if needed.
